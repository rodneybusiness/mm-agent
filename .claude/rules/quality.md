# Quality Conventions

- **Small diffs**: Prefer focused PRs over sprawling changes
- **TypeScript strict**: Use proper types, avoid `any`
- **Tool definitions are contracts**: Changing tool schemas affects agent behavior
- **Explicit over clever**: Readable code beats clever code
- **Delete dead code**: Don't comment out; remove entirely
