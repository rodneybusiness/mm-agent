# Testing Conventions

- **Tests needed**: This project lacks proper tests - add them when modifying code
- **Tool behavior is testable**: Mock Claude responses, test tool execution
- **Integration over mocks when cheap**: Real tool calls > complex mocks
- **Test edge cases**: Invalid inputs, API failures, timeout handling
